apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vayobyzh.fullname" . }}-config
  labels:
    {{- include "vayobyzh.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
